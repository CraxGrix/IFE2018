export default class Dishes {
    constructor(dishesList) {
        this.dishesList = []
        for(let dishes of dishesList) {
            let dishesObj = {
                name: dishes.name,
                
            }
            this.dishesList.push()
        }
    }
}